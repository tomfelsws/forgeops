image:
  repository: registry.gitlab.com/swissid/$CI_PROJECT_NAME/ds

instance: configstore

configstore:
  enabled: true

baseDN: "$CONFIGSTORE_BASE_DN"

replicas: $CONFIGSTORE_REPLICAS

#resources:
#  limits:
#    cpu: 1000m
#    memory: 1Gi
#  requests:
#    cpu: 1000m
#    memory: 1Gi

storageSize: 1Gi
#storageClass: fast
persistence: false

topologyKey: "$CONFIGSTORE_TOPOLOGY"
podAntiAffinity: $CONFIGSTORE_AFFINITY

backup:
#  pvcClaimName: ds-backup
  clusterName: $CI_ENVIRONMENT_SLUG
  verify: false

# If existing database is found then restore is always skipped
restore:
  enabled: false

securityContext:
